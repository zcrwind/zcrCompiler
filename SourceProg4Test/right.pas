program zcrProgram;
var i, j, k: integer;
    number, sum: real;
// var color: real;
begin
	i := -7;
	k := i + 1;
	// p := 404;
	if (i <= 20) then i := i + 1;

	while number > 0 do
	begin
	   sum := 200;
	   number := 26;
	   sum := sum + number;
	   number := number - 2;
	end;
	
	j := (9 * 3) - k;
	for i := 1 to 10 do
	begin
		j := (j + 2) * ((i + 6) - 10) + 4;
	end;
end.